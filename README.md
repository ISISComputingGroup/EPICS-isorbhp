# isorb-hp

Minimal control automation for an Anton-Paar iSorb HP device.

## Dependencies

- `git` - install from https://git-scm.com/install/windows
- `uv` - installed using `winget install --id=astral-sh.uv -e`

## Running

`uv run isorb.py`